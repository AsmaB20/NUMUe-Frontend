# NUMUe Frontend — IDEF0 Function Analysis

**Platform:** NUMUe Educational Platform (Next.js / React)
**Base URL:** https://aiassistant.qcri.org/
**Date Reviewed:** 2026-05-17

---

## Legend

| Tag | Module |
|-----|--------|
| `[AUTH]` | Authentication |
| `[API]` | API Layer |
| `[UI]` | UI Component |
| `[HOOK]` | React Hook |
| `[UTIL]` | Utility / Store |

---

## Table of Functions

| # | Function | Input | Output | Control | Mechanism | Sub-functions | Errors / Improvements | Linkage |
|---|----------|-------|--------|---------|-----------|---------------|-----------------------|---------|
| **1** | **loginWithCredentials** `[AUTH]` `Z component / x4 API` | Email, password, optional OTP string | JWT token stored in localStorage; auth state updated; redirect to root | React Hook Form validation; OTP visibility toggle state | Axios (POST /auth/login); react-query mutation; react-toastify; useRouter | getAuthToken (9), validateEmail (44), useLoginMutation | ✗ `console.log` in error handler leaks to prod ✗ No brute-force / rate-limit protection ✗ OTP field still submittable with empty value → Move console.log behind dev flag; add lockout after N failures | → 2, 9, 44, 35 |
| **2** | **loginWithGoogle** `[AUTH]` `useGoogleLogin / u9 API` | Google access_token (OAuth); optional role, name | JWT + user stored; redirect to root; or redirect to signup if "pending" | @react-oauth/google hook; pending-account branch logic | Axios (POST /auth/google); sessionStorage for pending token; useRouter | useGoogleAuthMutation, loginWithCredentials (1) | ✗ If tab is closed during "pending" flow, sessionStorage token is lost — broken redirect ✗ 403 inactive-account error not distinguished from generic error in all branches → Persist pending token to localStorage; add specific messaging per error code | → 1, 3, 9 |
| **3** | **registerUser** `[AUTH]` `F component / IU API` | email, password, name, role (teacher/student/school_admin) | New user created; OTP verification screen shown; success toast | React Hook Form; agree checkbox required; Google signup flag | Axios (POST /auth/register); react-query mutation; react-toastify | OTPVerificationForm (5), validateEmail (44), useRegisterMutation | ✗ Multiple debug `console.log` calls in error handler (prod leakage) ✗ `O()` and `L()` helper functions are inline — not reusable ✗ Password min-length hint only visible after error, not upfront → Extract O/L to utils; show password requirements inline before submit | → 4, 5, 44 |
| **4** | **activateAccount (OTP)** `[AUTH]` `C mutation / R$ API` | userId string, 6-digit OTP array joined to string | Account activated; redirect to /login; success toast | 6-cell input validation; digit-only regex on change | Axios (POST /auth/:id/activate); useRef array for inputs | resendOTP (5), OTPVerificationForm (5) | ✗ No countdown timer shown for OTP expiry ✗ No lockout after repeated failed OTP attempts ✗ Paste handler strips non-digits but may not handle fewer than 6 chars cleanly on all browsers → Add visible expiry countdown; limit attempts to 3 then force resend | → 5, 1 |
| **5** | **resendOTP** `[AUTH]` `E mutation / jt API` | userId string | New OTP sent to user email; success/error toast | Button disabled while isPending | Axios (GET /auth/:id/activate); react-query mutation | — | ✗ No cooldown timer between resend attempts — user can spam the endpoint ✗ onSuccess only has `console.log` — no UI reset of digit cells → Add 60s cooldown; auto-clear digit cells on resend | → 4 |
| **6** | **requestPasswordReset** `[AUTH]` `T page / f8 API` | Email string | Reset OTP sent; redirect to /reset-password with email in query | Email validation; React Hook Form | Axios (GET /auth/reset-password/:email); react-toastify; useRouter | validateEmail (44) | ✗ Uses GET with sensitive data (email) in URL path — prefer POST ✗ onSuccess only logs to console → Switch to POST for password reset request; confirm email step in UI | → 7, 44 |
| **7** | **resetPassword** `[AUTH]` `c0 API call` | Email/userId, OTP, new_password | Password updated; redirect to login | OTP input + new password field validation | Axios (POST /auth/reset-password/:email); react-query mutation | getAuthToken (9) | ✗ Email appears in URL path — may be logged by servers/proxies ✗ No password-confirm field — user cannot verify typed password → Add confirm-password field; use POST body for email | → 6, 1 |
| **8** | **logout** `[AUTH]` `kS API call` | Auth token from localStorage | Session invalidated server-side; localStorage cleared; auth state reset | Token presence check (throws if missing) | Axios (POST /auth/logout); localStorage.removeItem | getAuthToken (9) | ✗ If token is already expired, the throw from getAuthToken (9) is unhandled in logout flow ✗ No redirect after logout in the API function itself — relies on callers → Wrap getAuthToken call in try/catch; ensure callers redirect consistently | → 9 |
| **9** | **getAuthToken** `[UTIL]` `sK utility` | localStorage | Auth token string | Throws Error if token missing | `localStorage.getItem("authToken")` | — | ✗ Token stored in localStorage — XSS-vulnerable; no refresh-token mechanism ✗ Throws synchronously inside async callers without consistent catch handling ✗ No token expiry check before returning → Consider httpOnly cookies; add token expiry validation; catch uniformly in callers | → 1, 8, 10, 11, 22 |

---

## API Layer

| # | Function | Input | Output | Control | Mechanism | Sub-functions | Errors / Improvements | Linkage |
|---|----------|-------|--------|---------|-----------|---------------|-----------------------|---------|
| **10** | **fetchStudentAssignments** `[API]` `b._O API` | studentId, filter params (status, is_public) | Assignment array from server | Auth token header; staleTime 300s; refetchOnWindowFocus | Axios (GET /students/:id/assignments); react-query | getAuthToken (9) | ✗ 401 handler removes token and redirects but other errors (500, 404) are silently ignored ✗ No error boundary for non-401 API failures → Handle 404 and 5xx with user-facing error state; add retry config for transient errors | → 9, 11 |
| **11** | **useStudentAssignments** `[HOOK]` `w hook` | studentId, filter string (status) | `{ assignments[], isPending, isRefetching, error }` | enabled: !!studentId; 401 triggers logout + invalidate | react-query useQuery; useRouter for redirect | fetchStudentAssignments (10) | ✗ `console.log("Student Assignments:", h)` in consuming component — prod log ✗ 401 handling duplicated across this hook and usePublicAssignments (15) — no shared util → Extract 401 handler to shared interceptor; remove debug log | → 10, 12, 17 |
| **12** | **fetchAssignments (teacher)** `[API]` `b.Oh API / $ hook` | `{ teacherId, title, is_public }` filter params | Assignment array (teacher's own assignments) | staleTime 3600s; retry 1; 401 redirect | Axios (GET /assignments); react-query; getAuthToken (9) | getAuthToken (9), useDebounce for title | ✗ `console.log("Assignments fetched:", r)` in useEffect — prod log ✗ useRef "logged" flag pattern prevents re-logging but adds unnecessary complexity → Remove debug useRef/log pattern entirely | → 9, 27 |
| **13** | **fetchStudentInsights** `[API]` `o API / useInsights hook` | teacherId, optional filter params | Insights object: `{ summary: { total_students, average_student_score, assignment_completion_rate } }` | enabled: !!teacherId; retry 1 | Axios (GET /insights/teacher/:id); react-query | getAuthToken (9) | ✗ No staleTime set — data refetches on every focus/mount ✗ Null coalescing falls back to 0 silently, masking real loading vs empty state → Set staleTime; differentiate null (loading) from 0 (genuinely no students) | → 9, 24 |
| **14** | **fetchStudentAchievements** `[API]` `m API / useStudentAchievements hook` | studentId | Achievement array: `{ id, url, title }[]` | enabled: !!studentId; staleTime 3600s | Axios (GET /students/:id/achievements); react-query | getAuthToken (9) | ✗ No error state rendered — achievements silently empty on API failure → Add error fallback in StudentProfilePanel (23) | → 9, 23 |
| **15** | **fetchPublicAssignments** `[API]` `b.Oh with is_public=true / $ hook` | `{ is_public: true }` | Public assignment array | staleTime 3600s; 401 redirect | Axios; react-query; getAuthToken (9) | getAuthToken (9), startPublicAssignment (16) | ✗ Sort `(s.id - e.id)` runs on every render — not memoized ✗ 401 handling duplicated (same pattern as fn 11) → Wrap sort in useMemo; deduplicate 401 handling | → 9, 16, 17 |
| **16** | **startPublicAssignment** `[API]` `K mutation / b.Wb` | `{ studentID, assignmentID }` | Assignment started server-side; redirect to assignment page | "Assignment already started" error is handled silently (redirect anyway) | Axios (POST); react-query mutation; useRouter | getAuthToken (9) | ✗ "Assignment already started" treated as non-error with same success redirect — could mask real errors ✗ No loading state per card (pointer-events-none but no spinner) → Differentiate error types; show per-card loading state | → 9, 18, 15 |
| **17** | **assignHomework** `[API]` `eP mutation / b.SU` | `{ teacherID, assignmentID, data: { due_date, class_id, student_ids[] } }` | Assignment assigned; queries invalidated + refetched | disabled if no class and no students selected | Axios (PATCH/PUT); react-query mutation + invalidateQueries + refetchQueries | getAuthToken (9), AssignToClassModal (31) | ✗ Both `invalidateQueries` AND `refetchQueries` called on success — causes double network request ✗ class_id falls back to `0` (null ?? 0) — sending 0 as class_id may be ambiguous to API → Remove redundant refetchQueries (invalidate alone triggers refetch); use null not 0 for no class | → 9, 31, 27 |
| **18** | **fetchSchoolAssignments** `[API]` `e2 hook / b.QO` | teacherId | School-wide assignment array | staleTime: 0 — always refetch on mount | Axios (GET /school-assignments); react-query; refetchOnWindowFocus true | getAuthToken (9) | ✗ staleTime:0 means every component mount triggers a fresh network call — expensive ✗ refetchOnWindowFocus:true with staleTime:0 doubles requests on tab switches → Set staleTime to at least 30s; review if real-time freshness is actually needed | → 9, 28 |

---

## Student UI Components

| # | Function | Input | Output | Control | Mechanism | Sub-functions | Errors / Improvements | Linkage |
|---|----------|-------|--------|---------|-----------|---------------|-----------------------|---------|
| **19** | **AssignmentCard** `[UI]` `L component` | assignment object (status, score, due_date, type, tags) | Rendered card with badges, preview text, CTA button | Status enum (NOT_STARTED, STARTED, SUBMITTED, GRADED); assignment type (Writing/Reading) | DOMPurify.sanitize for HTML content; Next/Image; formatDate (43) | formatDate (43), StatusBadge, ImageMapper | ✗ HTML content truncated to 155 chars after sanitize — could cut mid-tag creating broken preview ✗ `dangerouslySetInnerHTML` with truncated sanitized HTML risks visual artifacts → Truncate before sanitize, or use a text-only preview field; strip HTML tags for preview | → 17, 20, 43 |
| **20** | **FilterBar** `[UI]` `Q component` | onFilterChange, onSortChange, onPublicViewChange callbacks; sort value | Rendered filter buttons (all / in-progress / complete / new / public) + sort dropdown | Local activeFilter state; public view toggle | useState; Button components; SortDropdown | — | ✗ Public filter sets active state to literal string `"public"` instead of the `Io` enum — inconsistent with other filter values ✗ Filter state is duplicated (local + parent) — could desync → Use `Io.PUBLIC` enum value; lift filter state fully to parent or fully to child | → 17, 21 |
| **21** | **StudentHomePage** `[UI]` `H component` | User state (id, hasSeenOnboarding); filter, sort states | Rendered assignment list with filter bar; public/private toggle animation | showPublicView boolean state; sort string state; AnimatePresence mode="wait" | useStudentAssignments (11); Framer Motion; useStore; useCallback memoization | FilterBar (20), AssignmentCard (19), PublicAssignmentsList (26), StudentOnboarding (38) | ✗ `console.log("Student Assignments:", h)` — prod debug log ✗ `console.log("Sort:", e)` — prod debug log ✗ Due-date sort uses `localeCompare` on ISO strings — works by coincidence, not semantics ✗ useEffect clears panel on every render when `t !== ""` — risky dependency → Remove logs; use Date comparison for due_date sort; guard panel-clearing effect | → 11, 19, 20, 26, 38 |
| **22** | **NotificationsPanel** `[UI]` `ee component` | userId (from store) | Notification list; "See All" modal | isModalOpen boolean state; sorted by id desc | useNotifications hook; Modal component; Spinner | useNotifications, NotificationItem | ✗ Notifications sorted by id desc on every render — not memoized with useMemo ✗ Same sorted list rendered twice (panel + modal) without shared memoization → Memoize sorted list; share between panel and modal views | → 21 |
| **23** | **StudentProfilePanel** `[UI]` `ec component` | userId, avatar, name, hasSeenOnboarding (from store) | Avatar, name, skills radar chart, achievements list, insights button | hasSeenOnboarding gates real vs mock data; chart options computed via eo() | useStudentAchievements (14), useStudentSkills hook; dynamic import RadarChart; useMemo | calculateChartOptions, useStudentSkills, useStudentAchievements (14) | ✗ Skills radar uses a dynamically imported component (lazy) but has no explicit Suspense boundary — falls through to empty state ✗ useMemo for chartData includes translation function (u) as dep — causes recompute on locale change even for same data → Wrap lazy chart in Suspense; separate i18n label memoization from data memoization | → 14, 21, 43 |
| **24** | **RecentResourcesPanel** `[UI]` `eg component` | userId (from store) | List of up to 3 recent resources with file-type icons and truncated filenames | hasSeenOnboarding gates data; slice(0,3) | useStudentResources hook; file extension detection; Next/Link | useStudentResources (45), getFileExtension, truncateFilename | ✗ File extension check uses a long if-else chain — not a lookup map ✗ Falls back to "docfile" icon for unknown types — same icon for pptx and unknown → Use an extension→icon map object; add explicit pptx case | → 21, 45 |
| **25** | **PublicAssignmentCard** `[UI]` `q component` | assignment object (title, content) | Rendered public assignment card (no status/score) | Read-only; onClick handled by parent PublicAssignmentsList (26) | DOMPurify.sanitize; Next/Image | — | ✗ Same 155-char post-sanitize truncation issue as AssignmentCard (19) ✗ No visual loading state when card is clicked and mutation isPending → Apply same fix as fn 19; add per-card pending indicator | → 26, 19 |
| **26** | **PublicAssignmentsList** `[UI]` `W component` | `{ is_public: true }` filter | Sorted public assignment list; triggers navigation on click | isPending (mutation) disables all cards; sort by id desc | fetchPublicAssignments (15), startPublicAssignment (16), useStore, useRouter | PublicAssignmentCard (25), startPublicAssignment (16) | ✗ Sort runs inline `.sort()` on returned array — mutates cached react-query data in place; should use `[...data].sort` ✗ Assignment URL routing logic duplicated here and in TeacherAssignmentList (27) → Use spread before sort; extract `getAssignmentUrl()` to shared utility | → 15, 16, 25 |

---

## Teacher UI Components

| # | Function | Input | Output | Control | Mechanism | Sub-functions | Errors / Improvements | Linkage |
|---|----------|-------|--------|---------|-----------|---------------|-----------------------|---------|
| **27** | **TeacherAssignmentList** `[UI]` `eJ component` | assignments[], isPending, isRefetching, listView bool, filter string | Assignment grid/list with assign buttons; opens AssignToClassModal (31) | Sort by creation_date or due_date; listView toggle; disabled during refetch | AssignmentCard (teacher variant), AnimatePresence, useStore | AssignToClassModal (31), AssignmentCardTeacher | ✗ Inline `.sort()` mutates the prop array — same issue as fn 26 ✗ assignedStudentIds extraction logic duplicated in both eJ and e$ (SharedList fn 28) → Spread before sort; extract `getAssignedIds()` helper shared by both | → 12, 31, 32 |
| **28** | **SharedAssignmentList** `[UI]` `e$ component` | schoolAssignments[], isPending, isRefetching, listView, filter | School-wide shared assignments grid; same assign modal | Same sort/filter logic as TeacherAssignmentList (27) | AssignmentCard (shared=true variant), AssignToClassModal (31) | AssignToClassModal (31), AssignmentCardTeacher | ✗ Near-identical code to TeacherAssignmentList (27) — significant DRY violation ✗ Empty state uses different image (`/no-results.png` vs `/no-found.png`) — inconsistent UX → Merge eJ and e$ into one parameterized component; standardize empty-state assets | → 18, 27, 31 |
| **29** | **SubmittedAssignmentList** `[UI]` `eB component` | recentlySubmitted[], listView, isPending, isRefetching | Grid of submitted assignments awaiting review; sorted by assignment.id desc | listView toggles grid columns; title search filter applied in parent | ReviewCard (eK); useRecentlySubmitted hook (46) | ReviewCard | ✗ Sort key `assignment.id` is not the same as submission time — order may be misleading ✗ No empty-state distinction between "no submissions" and "filtered to zero" → Use `submitted_at` timestamp for sort; differentiate empty vs filtered-empty state | → 32, 46 |
| **30** | **GradedAssignmentList** `[UI]` `eU component` | recentlyReviewed[], listView, isPending, isRefetching | Grid of graded assignments; sorted by assignment.id desc | listView; title filter in parent; i18n via `eH.$G` (different import) | GradedCard (eW); useRecentlyReviewed hook (47) | GradedCard | ✗ Uses `eH.$G` (different i18n import than sibling components using `C.$G`) — potential mismatch if namespaces diverge ✗ Same structural duplication as eB (fn 29) → Normalize i18n import across all teacher list components; consider shared ListContainer component | → 32, 47 |
| **31** | **AssignToClassModal** `[UI]` `eq component` | open bool, assignmentID, assignedStudentIds[], onClose callback | Modal with class/student tabs, date picker; triggers assignHomework (17) | Tab selection; class vs individual student toggle; date picker calendar open state | useClasses, useStudents hooks; react-datepicker; useStore for selections | assignHomework (17), DatePicker, ClassList, StudentList | ✗ Close handler calls `removeAssignStudent` individually for each student O(n) instead of a bulk reset action ✗ Date picker stays open if user closes modal without selecting — no onClose passed to calendar ✗ Search filter only on student tab, not class tab → Add `resetAssignStudents()` bulk action to store; close calendar on modal dismiss; add class search | → 17, 27, 28 |
| **32** | **TeacherAssignmentTabs** `[UI]` `e5 component` | teacherId, hasSeenOnboarding (from store) | Tabbed view: Created / Submitted / Graded / Shared assignments | activeTab index state; listView bool; sort string; debounced search string | AnimatePresence; useDebounce; useCallback; useStore | TeacherAssignmentList (27), SubmittedAssignmentList (29), GradedAssignmentList (30), SharedAssignmentList (28), FilterToolbar (33) | ✗ Search filter for "submitted" and "graded" tabs runs client-side on cached data — inconsistent with "created" tab which uses server-side debounced search ✗ Onboarding mock data injected at this level, tightly coupling data fetching with onboarding logic → Unify search strategy; decouple onboarding mock data to a separate hook | → 12, 18, 27, 28, 29, 30, 33 |
| **33** | **FilterToolbar (teacher)** `[UI]` `eV component` | listView, onListView, onFilter, onSearch, filter callbacks | List/grid toggle buttons, sort dropdown, search input | Controlled by parent (e5); no local state | Button, Dropdown, Input components | — | ✗ onSearch is passed but search input debounce is handled in parent — unclear ownership ✗ No clear/reset button for search field → Own debounce inside the toolbar; add a clear (×) button when input has value | → 32 |
| **34** | **TeacherDashboardCards** `[UI]` `eE component` | teacherId (from store), hasSeenOnboarding | 3 stat cards: totalStudents, averageScore%, completionRate% | `o` flag hides stats when onboarded but no students yet; mock data for onboarding | useInsights (13); conditional NoInsightsPlaceholder | useInsights (13), NoInsightsPlaceholder | ✗ Mock data hardcoded (28 students, 84% score, 77% completion) — will look wrong if layout changes ✗ `o` flag logic is a complex boolean negation — hard to read → Move mock data to a constants/fixtures file; simplify boolean with named variables | → 13, 35 |
| **35** | **TeacherHomeDashboard** `[UI]` `e3 / e8 component` | — | Full teacher dashboard: header, stats cards, assignment tabs, onboarding tour | Rendered only for role=teacher (from HomePage fn 36) | PageLayout, TeacherPageHeader, TeacherDashboardCards (34), TeacherAssignmentTabs (32) | TeacherDashboardCards (34), TeacherAssignmentTabs (32), TeacherOnboardingTour (40) | ✗ `clearQuestions()` called in useEffect on mount — may clear in-progress student state if teacher navigates briefly → Confirm clearQuestions is only relevant for student context; guard with role check | → 32, 34, 40 |
| **36** | **HomePage (root)** `[UI]` `se component / index page` | user.role, isAuthenticated (from auth hook) | Routes to TeacherHomeDashboard (35) or StudentHomeDashboard (37); redirects school_admin | role === "teacher" / "school_admin" branches; useEffect for admin redirect | useAuthStore; useRouter; Layout components | TeacherHomeDashboard (35), StudentHomeDashboard (37) | ✗ school_admin redirect uses `window.location.href` (hard reload) instead of `router.push` — breaks SPA navigation and clears client state ✗ Returns `null` when `!isAuthenticated` — no loading state, causes layout flash → Use `router.push` for school_admin redirect; show loading skeleton while auth resolves | → 35, 37 |
| **37** | **StudentHomeDashboard** `[UI]` `ek component` | — | Full student dashboard: profile panel, assignments, notifications, resources, footer | Rendered only for role=student (from HomePage fn 36) | Grid layout (12 cols); clearQuestions on mount; Footer with Fanar link | StudentProfilePanel (23), StudentHomePage (21), NotificationsPanel (22), RecentResourcesPanel (24), StudentOnboardingTour (38) | ✗ `clearQuestions` in useEffect — same issue as fn 35 ✗ No skeleton/loading state for entire dashboard layout during initial auth resolution → Guard clearQuestions; add layout-level loading skeleton | → 21, 22, 23, 24, 38 |

---

## Onboarding

| # | Function | Input | Output | Control | Mechanism | Sub-functions | Errors / Improvements | Linkage |
|---|----------|-------|--------|---------|-----------|---------------|-----------------------|---------|
| **38** | **StudentOnboardingTour** `[UI]` `eZ component` | hasSeenOnboarding, showWelcomeModal (from store); locale from router | Joyride 5-step tour over 5 targets; Welcome modal; marks onboarding complete | localStorage "firstLogin" flag; run/stepIndex state; CLOSE action ends tour | react-joyride; localStorage; useCallback; useMemo for steps; setHasSeenOnboarding | StudentOnboardingWelcome (39), OnboardingTooltip (41) | ✗ `localStorage.getItem("firstLogin")` called directly in component body on every render — should be in useState initializer ✗ 4 debug `console.log` calls — prod logs ✗ Tour targets use `data-onboarding` attributes — if elements unmount before tour reaches them, TARGET_NOT_FOUND advances silently → Read localStorage in useState initializer; remove debug logs; handle TARGET_NOT_FOUND with user-facing message | → 37, 39, 41 |
| **39** | **StudentOnboardingWelcome** `[UI]` `eb component` | onFinish, onStartTour callbacks | Full-screen welcome modal rendered via React Portal into document.body | Rendered only when showWelcomeModal is true | ReactDOM.createPortal; Next/Image (priority); i18n | — | ✗ Uses `document.body` as portal target — if SSR runs this, `document` is undefined ✗ No keyboard trap (focus management) inside the modal — WCAG 2.1 accessibility violation → Guard with `typeof window !== "undefined"`; add focus-trap for accessibility | → 38 |
| **40** | **TeacherOnboardingTour** `[UI]` `e4 component` | hasSeenOnboarding, showWelcomeModal (from store); locale from router | Joyride 5-step tour for teacher UI; Welcome modal; spotlight transforms on steps 2 & 3 | Same localStorage + run/stepIndex logic as fn 38 | react-joyride; localStorage; custom spotlight transforms for specific steps | TeacherOnboardingWelcome, OnboardingTooltip (42) | ✗ Same `localStorage.getItem` on every render issue as fn 38 ✗ Same 4+ debug `console.log` calls ✗ Spotlight transform overrides (scaleY, translateY) for steps 2 and 3 are fragile — will break if layout changes ✗ Entire tour logic duplicated from fn 38 (DRY violation) → Extract shared `useOnboardingTour` hook; remove logs; make spotlight offsets configurable per step | → 35, 42 |
| **41** | **StudentOnboardingTooltip** `[UI]` `ew component` | Joyride tooltip props (step, index, size, locale, continuous); onClose callback | Styled tooltip: title, content, step counter, next/complete button | locale determines font class (Arabic vs Latin); last step shows "complete" instead of "next" | react-joyride tooltip API; i18n; Button; Icon | — | ✗ Title split on `"\n"` for line breaks — fragile, depends on translation strings containing literal newlines ✗ Nearly identical to teacher tooltip (fn 42) — only max-width differs (256px vs 393px) → Use a shared TooltipBase component with maxWidth prop; use proper rich-text for multiline titles | → 38, 40 |
| **42** | **TeacherOnboardingTooltip** `[UI]` `e6 component` | Same as fn 41 | Wider styled tooltip (393px max-width) for teacher tour | Same logic as fn 41 | Same as fn 41 | — | ✗ Duplicate of fn 41 — DRY violation ✗ Uses `b2` body text level while fn 41 uses `b3` — inconsistent typography between student/teacher experiences → Merge with fn 41 via `maxWidth` + `textLevel` props | → 40, 41 |

---

## Utilities & Hooks

| # | Function | Input | Output | Control | Mechanism | Sub-functions | Errors / Improvements | Linkage |
|---|----------|-------|--------|---------|-----------|---------------|-----------------------|---------|
| **43** | **formatDate** `[UTIL]` `N utility (module 2812)` | ISO date string (YYYY-MM-DD); showDay bool; appendComma bool | Localized date string e.g. "January 5, 2025" | i18n month names from `n.ag` (global i18n instance) | i18n.t for month names; string split and parse | — | ✗ Month names fetched via optional chain on a global (`n.ag`) — if i18n not yet initialized, returns array of `undefined` values silently ✗ No validation of input format — malformed date string returns garbled output ✗ Day parsed with `parseInt(r, 10)` but no bounds checking → Add input validation; guard n.ag initialization; consider `Date.prototype.toLocaleDateString` instead | → 19, 23 |
| **44** | **validateEmail** `[UTIL]` `v utility (module 97801)` | Email string | Boolean (valid/invalid) | Regex: `/^[^\s@]+@[^\s@]+\.[^\s@]+$/` | `RegExp.test()` | — | ✗ Regex allows `"a@b.c"` (single-char TLD) and blocks valid RFC 5322 edge cases ✗ Does not catch Unicode domain names (IDN) which are valid → Consider a battle-tested library (e.g. `validator.js isEmail`); or accept and let the server validate | → 1, 3, 6 |
| **45** | **useStudentResources** `[HOOK]` `_ hook (module 20691)` | studentId | `{ data: ResourceItem[], isPending }` | enabled: !!studentId && !onboarding_pending; staleTime 3600s; returns mock data during onboarding | react-query; fetchStudentResources API; onboarding mock fixture | fetchStudentResources API | ✗ Hook checks `!hasSeenOnboarding` to disable query but inverted guard is confusingly named ✗ Empty array fallback hides difference between "loading" and "no resources" → Rename flag for clarity; return a distinct `isLoading` vs `isEmpty` state | → 24 |
| **46** | **useRecentlySubmitted** `[HOOK]` `H hook (module 1662)` | teacherId | `{ recentlySubmitted: SubmittedAssignment[], isPending }` | enabled: !!teacherId; staleTime 3600s; retry 1 | react-query; Axios GET /recently-submitted/:id | getAuthToken (9) | ✗ staleTime 3600s for "recently submitted" is very long — a submission within that hour won't appear without manual refetch ✗ No `refetchOnWindowFocus` — teacher won't see new submissions after returning to tab → Reduce staleTime to 60–120s; enable refetchOnWindowFocus | → 9, 29, 32 |
| **47** | **useRecentlyReviewed** `[HOOK]` `E hook (module 24544)` | teacherId | `{ recentlyReviewed: ReviewedAssignment[], isPending }` | enabled: !!teacherId; staleTime 3600s; retry 1 | react-query; Axios GET /recently-reviewed/:id | getAuthToken (9) | ✗ Same staleTime concern as fn 46 ✗ No real-time or polling update — graded items may not refresh after teacher grades from another session → Lower staleTime; consider react-query polling (`refetchInterval`) for active grading sessions | → 9, 30, 32 |
| **48** | **assignmentStore (Zustand)** `[UTIL]` `module 33927` | Actions: setTasks, addAnswer, addTaskAnswer, setCurrentQuestion, resetAnswers, clearQuestions | Persistent assignment state: tasks, answers, currentQuestion, submittedQuestions | Zustand devtools + persist middleware | zustand; immer (via mW); localStorage via persist | clearQuestions (called in fn 35, 37) | ✗ `addTaskAnswer` merges by id using spread + filter — creates a new array on every call O(n) ✗ `clearQuestions` called on teacher dashboard mount (fn 35) — not appropriate for teacher context → Use a Map/object keyed by id for O(1) updates; restrict `clearQuestions` to student assignment pages only | → 21, 35, 37 |

---

## Critical Issues Summary

| Priority | Issue | Affected Functions |
|----------|-------|--------------------|
| 🔴 High | **12+ debug `console.log` calls** in production build | 3, 5, 11, 12, 21, 38, 40 |
| 🔴 High | **Auth token in localStorage** — XSS-vulnerable, no expiry check | 9, 1, 2 |
| 🔴 High | **`window.location.href` hard reload** for school_admin redirect — breaks SPA | 36 |
| 🟠 Medium | **Array mutation in `.sort()`** on react-query cached data — causes subtle render bugs | 15, 26, 27, 28 |
| 🟠 Medium | **Duplicate component code** (DRY violations): TeacherList/SharedList, both onboarding tours, both tooltips | 27/28, 38/40, 41/42 |
| 🟠 Medium | **staleTime too long (3600s)** for recently-submitted/reviewed — teacher misses new submissions | 46, 47 |
| 🟠 Medium | **Double network request** on assignHomework success (invalidate + refetch both called) | 17 |
| 🟡 Low | **Post-sanitize HTML truncation** at 155 chars — may cut mid-tag | 19, 25 |
| 🟡 Low | **No Suspense boundary** around lazy-loaded RadarChart component | 23 |
| 🟡 Low | **`document.body` portal without SSR guard** in onboarding welcome modal | 39 |
| 🟡 Low | **No focus-trap** in onboarding welcome modals — WCAG 2.1 violation | 39 |
| 🟡 Low | **Inconsistent i18n import** (`eH.$G` vs `C.$G`) across teacher components | 30 |