# Analysis Table

| # | Function / Area | Input | Output | Control | Mechanism | Errors / Risks Found | Ways To Improve | Sub Function | Linkage |
|---|----------------|-------|--------|---------|-----------|----------------------|-----------------|--------------|---------|
| 1 | Axios API client | API request config | Axios response/data | Global response interceptor | baseURL: https://aiassistant.qcri.org/ | Hard-coded API URL; logs full API errors to console, possibly exposing sensitive data | Move API URL to environment config; sanitize production logs | Auth, user, class, asset, insight API calls | 2, 3, 4, 9, 13 |
| 2 | getAuthToken | localStorage.authToken | Token string or thrown error | Throws if token missing | Reads browser localStorage | Token stored in localStorage is XSS-sensitive; thrown errors can break protected queries | Prefer httpOnly secure cookies or short-lived token + refresh flow; handle missing token through auth boundary | Used by all protected API wrappers | 1, 4, 9, 13 |
| 3 | Login form / login mutation | Email, password, optional OTP | Auth token, user, navigation | Form validation, mutation success/error | Calls auth/login, stores auth data | Error checks are inconsistent; uses console.log; token/role saved in localStorage | Centralize auth error handling; remove console logs; redirect only after auth state is confirmed | Email validator, Google auth, OTP flow | 1, 2, 5 |
| 4 | Google auth mutation | Google access token, role/name optional | Existing user auth or pending signup | If response has pending, redirects signup | Calls auth/google | Stores Google access token in sessionStorage; pending flow depends on client-side state | Minimize token lifetime; server-side OAuth exchange; handle refresh/back navigation safely | Signup Google completion | 3, 5 |
| 5 | Signup form | Name, email, password, role, agreement | Created user or OTP activation view | React Hook Form + mutation | Calls auth/register, then OTP activation | Role values are client-controlled; name regex is limited; duplicate error parsing is fragile | Enforce roles server-side; strengthen validation; map backend errors consistently | OTP component, Google signup | 1, 3, 4, 6 |
| 6 | OTP activation / resend | User ID, 6-digit OTP | Account activation, login redirect | 6 input boxes, paste handling, mutation | Calls auth/{id}/activate and resend endpoint | Accepts only digits, good; no resend cooldown; user ID comes from query | Add resend cooldown and attempt limits; hide user ID details where possible | Signup page | 5 |
| 7 | Reset password request | Email | Sends reset request and navigates reset page | Form validation + mutation | Calls auth/reset-password/{email} | Email is passed in URL query after request | Avoid exposing email in URL; use state or backend-issued reset flow token | Email validator | 1, 3 |
| 8 | Date formatter | YYYY-MM-DD string | Localized date string | String split and month lookup | Uses split("-"), translated month names | No validation; invalid date can produce undefined NaN output | Use Date parsing/date-fns with locale; validate input | Assignment cards | 10 |
| 9 | Student assignment hooks | Student ID, filter/status | Assignment array/query state | React Query | Calls student assignment APIs | Redirect/logout side effects run during render when query.error exists | Move redirect/localStorage cleanup into useEffect; centralize 401 handling in Axios interceptor | Student dashboard lists | 1, 2, 10 |
| 10 | Student assignment cards/list | Assignment objects | UI cards and links | Status/type branching | Sanitizes HTML then renders preview | Uses .sort() directly on query arrays, mutating cached data; repeated dangerouslySetInnerHTML still depends on sanitizer correctness | Use slice().sort(); keep sanitizer strict; prefer text previews if possible | Date formatter, public assignment start | 8, 9, 11 |
| 11 | Start public assignment | Student ID, assignment ID | Opens assignment route | Mutation success/error | Calls start assignment API | Handles only one specific backend error string: "Assignment already started" | Use backend error codes; handle generic failures visibly | Public assignment list | 10 |
| 12 | Notifications panel | User ID | Notification list/modal | React Query + local modal state | Fetches user notifications | Sort mutates array; no pagination visible | Use immutable sorting; add paging/infinite scroll if list grows | Student dashboard | 9 |
| 13 | Teacher class API wrappers | Teacher/class IDs, class payload | Class data/mutation result | REST calls with token | classes, teachers/{id}/classes endpoints | No local schema validation; repeated wrapper code across chunks | Type request/response contracts; share generated API client | Class page | 1, 2, 14 |
| 14 | Class list / create / rename / delete | Class name, class ID | Updated class UI | Modals + React Query invalidation | Mutations invalidate class queries | Empty/whitespace class names allowed; confirm modal closes before async result; direct .sort() mutates query data | Trim/validate names; disable submit until valid; close modal after success; immutable sort | Confirm modal, class API hooks | 13, 15 |
| 15 | Confirm modal | Open state, confirm/cancel callbacks | Modal UI | Portal to confirm-portal | Calls onConfirm() then onClose() | Async confirm is not awaited, so modal closes even if mutation fails | Await promise or let caller close on success | Delete/rename class | 14 |
| 16 | Content library upload | User ID, selected file | Uploaded resource asset | Dynamic <input type=file> + mutation | Calls users/{id}/assets multipart upload | Size check only; MIME/type trust relies on accept; console logs file/response | Validate MIME and extension; remove logs; show server validation errors | Asset query, upload mutation | 1, 2, 17 |
| 17 | Content library filtering/list | Assets, search, filters, sort | Resource cards | Client-side filter/sort/search | Filters type === "resource" and filename search | Possible cache mutation if sort helper mutates; key uses index in some maps | Ensure immutable helpers; use stable keys like asset ID | Asset query/upload | 16 |
| 18 | AI/dictionary service wrappers | Text/word/source | Translation, speech blob, image blob, dictionary data | Tokenized API calls | ai-services, ai-assistant, dictionary endpoints | Some functions return raw Axios promise while others return .data; inconsistent API shape | Normalize wrappers to always return typed data or blob | Dictionary/content/assistant features | 1, 2 |
| 19 | Insights filters | Timeframe, class, student | Query params and selected state | Zustand-like store + dropdowns | Builds class/student/time filters | Loading/error text is hard-coded English; auth error message says refresh only | Localize all text; reuse auth failure handler | Insights query | 20 |
| 20 | Insights query/chart transforms | Teacher ID, params, insights payload | Chart card data | React Query + useMemo transforms | Maps backend insight fields into charts | Many chart transforms silently show empty state if backend shape changes; scores not visibly clamped | Validate response shape; default missing numeric values; clamp percentages | Bar chart, insight cards | 19, 21 |
| 21 | Insights bar chart | Top students {name, score} | Chart.js bar chart | Slices top 3 students | Chart.js dataset/options | Only top 3 shown; no empty array distinction from loading/no data | Make limit explicit; show "no matching students" vs loading separately | Insights section cards | 20 |
| 22 | Authenticated page guards | Auth state | Page content or null | isAuthenticated check | Protected pages return null if unauthenticated | No visible loading/redirect fallback in several pages | Add consistent auth gate: loading, unauthorized, redirect | Teacher/student pages | 2, 9, 13 |
| 23 | Logging/debug statements | Runtime state/errors | Browser console output | console.log/error | Scattered in login, signup, assignments, onboarding, class | Leaks implementation details and possibly user/API data | Strip logs in production or gate behind debug flag | Many components | 1, 3, 5, 9, 14 |
| 24 | Bundle/source quality | Production JS chunks | Browser app | Next.js minified bundles | No source maps/source TS available in repo | Hard to maintain, test, or audit precisely | Review original source repo; add TypeScript, linting, unit tests, source maps for debugging | Whole frontend | All |